import javax.swing.*;
import java.awt.*;

public class Main {
    public static void main(String[] args) {

        new Window();

        /* Flyttat till WIndow klassen
        JFrame window= new JFrame(); // skapar "ramen"
        window.setVisible(true);  // gör den synlig
        window.setTitle("TE21C Clicker"); // Sätter en text på rutan
        window.setBounds(100,150, 500,500); // Sätter position och storlek
        window.setResizable(false); // gör att vi inte får ändra storleken
        window.setDefaultCloseOperation(WindowConstants.EXIT_ON_CLOSE);
        window.setLayout(null);
        */
/*
        JPanel canvas= new JPanel();
        canvas.setBounds(50,50,100,100);
        //canvas.setBackground(Color.red);
        canvas.setBackground(new Color(250,17,188));
*/
        //window.add(canvas);





    }
}